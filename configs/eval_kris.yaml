benchmark: kris
manifest: data/benchmarks/manifests/kris_eval_manifest.jsonl
output_dir: outputs/eval/kris/debug
agent:
  mode: dry_run_or_served_policy
renderer:
  backend: external_editor_required
scoring:
  official: optional
  rise_critic: true
decontamination:
  benchmarks: data/benchmarks
  fail_on: high
